name: driver_rtlsdr
description: >
  Full RTL-SDR (RTL2832U) stack for Flutter/Android in one package: native
  USB/FFI driver (tuning, IQ streaming, WFM/NFM/AM/SSB demodulation, stereo,
  RDS, spectrum, recording, gain), a testable ChangeNotifier radio engine
  (controllers, presets), and a gqrx-inspired widget library (scope,
  waterfall, tuner, panels, screens). Reusable native core (C, GPLv2).
  Merged from driver_rtlsdr + core_rtlsdr + widget_rtlsdr.
version: 0.3.3
repository: https://github.com/reinanbr/driver_rtlsdr_flutter
issue_tracker: https://github.com/reinanbr/driver_rtlsdr_flutter/issues
topics:
  - android
  - usb
  - ffi
  - radio
  - sdr

environment:
  sdk: ^3.12.2
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter
  ffi: ^2.2.0
  path_provider: ^2.1.5
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  # Lets tests fake path_provider's platform channel (swap
  # PathProviderPlatform.instance) instead of touching a real device/
  # emulator — used to test the legacy-Android Downloads fallback in
  # RecordingController without mocking a raw MethodChannel.
  path_provider_platform_interface: ^2.1.2

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # This section identifies this Flutter project as a plugin project.
  # The 'pluginClass' specifies the class (in Java, Kotlin, Swift, Objective-C, etc.)
  # which should be registered in the plugin registry. This is required for
  # using method channels.
  # The Android 'package' specifies package in which the registered class is.
  # This is required for using method channels on Android.
  # The 'ffiPlugin' specifies that native code should be built and bundled.
  # This is required for using `dart:ffi`.
  # All these are used by the tooling to maintain consistency when
  # adding or updating assets for this project.
  plugin:
    platforms:
      android:
        package: com.rtlsdrmobile.driver_rtlsdr
        pluginClass: DriverRtlsdrPlugin

  # To add assets to your plugin package, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg
  #
  # For details regarding assets in packages, see
  # https://flutter.dev/to/asset-from-package
  #
  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # To add custom fonts to your plugin package, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts in packages, see
  # https://flutter.dev/to/font-from-package
